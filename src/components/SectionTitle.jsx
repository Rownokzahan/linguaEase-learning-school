const SectionTitle = ({ label }) => {
  return (
    <div className="py-2 px-4 text-3xl font-semibold mx-auto w-max border-y-4 border-dotted border-accent_2 mb-12">
      <h3>{label}</h3>
    </div>
  );
};

export default SectionTitle;
